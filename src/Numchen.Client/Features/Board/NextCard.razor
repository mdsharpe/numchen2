<div class="next-card">
    <label>Next:</label>

    @if (Value.HasValue)
    {
        <Card Number="Value.Value"></Card>
    }
    else
    {
        @:None
    }
</div>

@code {
    [Parameter]
    public int? Value { get; set; }
}
